import MobileNavigation from "./mobile-nav";

const Header = () => {
  return (
    <header className="bg-muted border-b p-2">
      <MobileNavigation />
    </header>
  );
};

export default Header;
